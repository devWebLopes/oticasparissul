{
  "buildCommand": "cd ../.. && pnpm install && pnpm run build",
  "outputDirectory": "dist/public",
  "installCommand": "echo 'install runs via buildCommand'",
  "framework": null,
  "rootDirectory": "artifacts/oticas-paris-sul",
  "rewrites": [
    { "source": "/((?!assets|favicon).*)", "destination": "/index.html" }
  ]
}
